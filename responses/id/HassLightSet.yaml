language: id
responses:
  intents:
    HassLightSet:
      brightness: "Mengatur kecerahan {{ slots.name }} menjadi {{ slots.brightness }}"
      brightness_area: "Mengatur kecerahan pada {{ slots.area }} menjadi {{ slots.brightness }}"
      color: "Mengatur warna {{ slots.name }} menjadi {{ slots.color }}"
      color_area: "Mengatur warna pada {{ slots.area }} menjadi {{ slots.color }}"
